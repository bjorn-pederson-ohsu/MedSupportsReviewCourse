package moduleParts{	import flash.display.MovieClip;	import flash.events.EventDispatcher;	import flash.events.Event;	import reflectionViewer.*;	import caurina.transitions.Tweener;	public class reflectionMain extends MovieClip {				private var model:reflectionModel		private var view:reflectionView		private var control:reflectionController				private var reflectionXml:XML		private var bgwidth:int;		private var bgheight:int;		public function reflectionMain(i:int,v:int,xml:XML) {			// constructor code			bgwidth = i;			bgheight = v;			reflectionXml = new XML(xml)			addEventListener(Event.ADDED_TO_STAGE,addedtostage);		}		private function addedtostage(event:Event):void {			removeEventListener(Event.ADDED_TO_STAGE,addedtostage);			addEventListener(Event.REMOVED_FROM_STAGE,removedFromStage);			model= new reflectionModel();			control = new reflectionController(model);			view = new reflectionView(model,control,reflectionXml, bgwidth, bgheight);			addChild(view);					}		private function removedFromStage(event:Event):void {			removeEventListener(Event.REMOVED_FROM_STAGE,removedFromStage);			removeChild(view) 			view=null		}	}}