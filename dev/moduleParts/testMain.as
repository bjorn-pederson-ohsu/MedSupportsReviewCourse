package moduleParts{	import flash.display.MovieClip;	import flash.events.EventDispatcher;	import flash.events.Event;	import testViewer.*;	import caurina.transitions.Tweener;	public class testMain extends MovieClip {				private var model:testModel		private var view:testView		private var control:testController				private var testXml:XML		private var bgwidth:int;		private var bgheight:int;		public function testMain(i:int,v:int,xml:XML) {			// constructor code			bgwidth = i;			trace (1)			bgheight = v;			trace(2)			testXml = new XML(xml)			//trace(testXml.point.length())			addEventListener(Event.ADDED_TO_STAGE,addedtostage);		}		private function addedtostage(event:Event):void {			removeEventListener(Event.ADDED_TO_STAGE,addedtostage);			addEventListener(Event.REMOVED_FROM_STAGE,removedFromStage);			model= new testModel();			control = new testController(model);			view = new testView(model,control,testXml, bgwidth, bgheight);			addChild(view);					}		private function removedFromStage(event:Event):void {			removeEventListener(Event.REMOVED_FROM_STAGE,removedFromStage);			removeChild(view) 			view=null		}	}}